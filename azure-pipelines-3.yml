
trigger:
- none

pool:
  name: my_agent_pool
parameters:
  - name: my_name
    type: string
    default: name

  - name: age
    type: string
    default: 0

variables: 
 greet: 'hello ${{ parameters.my_name }}'
 


stages:
- stage: one_stage
  jobs:
  - job: one_job
    steps:
    - script: echo $(greet)



   